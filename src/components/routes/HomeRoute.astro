---
import Layout from '@/layouts/Layout.astro'
import Monogram from '@/components/Monogram.astro'
import SocialLinks from '@/components/SocialLinks.astro'
import resume from '@/data/resume.json'
import { createTranslator, localePath } from '@/i18n'

const { locale } = Astro.props
const t = createTranslator(locale)
const bio = t('general.profile_description').split('\n').map((s) => s.trim()).filter(Boolean)
---
<Layout locale={locale} path="/" title={t('general.nav_about')} description={t('general.meta_description_home')}>
  <section class="flex flex-col items-center py-10 text-center md:py-20">
    <Monogram class="h-36 w-36 text-green-400" />
    <h1 class="mt-6 text-3xl font-semibold text-green-400">{resume.basics.name}</h1>
    <p class="mt-2 text-neutral-500 dark:text-neutral-400">
      {t('general.role_profile')}, <span class="rounded-sm bg-green-100 px-1 text-green-500 dark:bg-neutral-700 dark:text-green-400">{resume.basics.label}</span>
    </p>

    <SocialLinks profiles={resume.basics.profiles} />

    <div class="max-w-2xl space-y-4 text-neutral-600 dark:text-neutral-300">
      {bio.map((paragraph) => <p>{paragraph}</p>)}
    </div>

    <div class="mt-10 flex flex-wrap justify-center gap-4">
      <a class="btn" href={localePath(locale, '/projetos')}>{t('general.nav_projects')}</a>
      <a class="btn" href={localePath(locale, '/contato')}>{t('general.nav_contact')}</a>
    </div>
  </section>
</Layout>
